install:

before_script:
  - touch codeship-yaml-was-here
script:
  - jdk_switcher use oraclejdk8
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.host.url=$SONAR_URL -Dsonar.login=$SONAR_TOKEN
after_success:
  - echo "Now we can deploy 2"